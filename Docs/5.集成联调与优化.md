# 阶段五：集成联调与优化

> 开发状态：**已完成** ✅
> 完成日期：2026-04-30

***

## 1. 阶段目标

完成定时任务、自启动、系统托盘等集成功能，进行整体联调和优化，最终打包发布。

## 2. 任务完成情况

| 编号 | 任务 | 状态 | 说明 |
| --- | --- | --- | --- |
| 5.1 | 集成定时任务调度 | ✅ 已完成 | main.py中启动APScheduler定时调度 |
| 5.2 | 集成开机自启动 | ✅ 已完成 | 侧边栏开关与注册表操作联动，配置同步保存 |
| 5.3 | 实现系统托盘 | ✅ 已完成 | pystray实现托盘图标+右键菜单+通知气泡+双击恢复 |
| 5.4 | 实现首次运行引导集成 | ✅ 已完成 | 检测Token/API Key为空时自动弹出引导+示例规则创建 |
| 5.5 | 端到端联调 | ⚠️ 待验证 | 需配置GitHub Token和LLM API Key后验证完整推送流程 |
| 5.6 | 边界情况处理 | ✅ 已完成 | 核心模块6种边界情况+6种LLM容错已处理 |
| 5.7 | 性能优化 | ✅ 已完成 | API频率控制、超时处理、后台线程执行 |
| 5.8 | 打包配置 | ✅ 已完成 | PyInstaller build.spec + build.py |
| 5.9 | Bug修复 | ✅ 已完成 | 修复2个运行时Bug |
| 5.10 | UI全面重设计 | ✅ 已完成 | HashiCorp设计系统重构+按钮重做+语言多选+Bug修复 |

## 3. Bug修复记录

### Bug 1：TaskService.run_task()方法名不匹配

**问题**：`gui/app.py` 第169行调用了 `self._task_svc.run_task()`，但 `TaskService` 的方法名是 `run_task_now()`，导致 `AttributeError`。

**修复**：将 `self._task_svc.run_task()` 改为 `self._task_svc.run_task_now()`。

**文件**：`gui/app.py`

### Bug 2：CTkMessagebox不存在

**问题**：`gui/pages/settings_page.py` 中使用了 `ctk.CTkMessagebox()`，但 `CTkMessagebox` 不是 customtkinter 内置组件，是独立的第三方包，导致 `AttributeError: module 'customtkinter' has no attribute 'CTkMessagebox'`。

**修复**：在 `gui/components/widgets.py` 中新增自定义 `MessageBox` 组件，替换所有 `CTkMessagebox` 调用。

**影响文件**：
- `gui/components/widgets.py` — 新增 `MessageBox` 类
- `gui/pages/settings_page.py` — 替换4处 `CTkMessagebox` 调用

### Bug 3：大模型配置下拉框无效

**问题**：Provider下拉框初始化时仅包含硬编码的"火山方舟 Coding Plan"，未从配置中读取所有providers。选择provider后Base URL和模型未自动更新。保存时provider name到key的映射可能失败。

**修复**：
- 初始化时从`settings_svc.get_settings_for_edit()`读取所有providers的name列表
- `_on_provider_changed`回调中根据选中name查找provider key，自动更新Base URL和模型下拉框
- 保存时使用`_get_provider_key_by_name`正确映射

**影响文件**：`gui/pages/settings_page.py`

### Bug 4：API Key保存错误

**问题**：Provider下拉框选择无效导致provider key为None，保存时`llm.provider`被设为"custom"，后续加载无法正确还原provider配置。

**修复**：与Bug 3一并修复，确保provider name→key映射正确。

**影响文件**：`gui/pages/settings_page.py`

### Bug 5：推送规则表单无法修改保存

**问题**：编辑规则弹窗中，字段默认值可能未正确填入（特别是topics和keywords为列表时），导致表单显示为空，用户无法修改。

**修复**：
- 确保编辑弹窗正确预填所有字段（keywords/topics从列表转为逗号分隔字符串）
- 增大弹窗尺寸(460x540)并使用CTkScrollableFrame确保所有字段可见
- 添加错误标签显示验证失败原因

**影响文件**：`gui/pages/rules_page.py`

### Bug 6：定时执行时间输入无格式校验

**问题**：执行时间使用普通CTkEntry，用户可输入任意文本，无时间格式校验。

**修复**：新增`TimePicker`组件，使用小时(00-23)+分钟(00-55,步长5)下拉框替代文本输入。

**影响文件**：`gui/components/widgets.py`、`gui/pages/settings_page.py`

### UI重设计（5.10）

**变更范围**：全面重设计GUI界面（HashiCorp Design System风格）

**主要变更**：
- **配色方案**：从Cyan-Blue(#00B4D8)更改为HashiCorp黑蓝风格(ACTION_BLUE=#1868f2)
- **背景色**：从#DBDBDB更改为#f1f2f3(Light Gray)，**移除所有RGB(235,235,235)灰色**
- **侧边栏**：Near Black(#0d0e12)，双模式一致
- **按钮系统**：4种按钮类型（PrimaryButton/SecondaryButton/DangerButton/GhostButton）
- **自动保存**：移除"保存设置"按钮，所有表单字段输入完即刻保存
- **时间选择器**：替代文本输入框
- **Provider下拉框**：修复初始化和回调
- **语言多选**：新增LanguageMultiSelect组件，支持26种GitHub常用语言
- **优先级滑块**：修复数字与滑块不同步，数字标签在左侧实时跟随
- **TagLabel**：新增标签组件，12%透明度背景+彩色文字
- **固定顶部栏**：新增52px固定顶部栏显示页面标题
- **表单宽度**：统一为SHORT(90px)/MEDIUM(180px)/LONG(320px)三档
- **圆角**：HashiCorp风格极简克制（卡片6px/按钮4px/输入框4px）

**Bug修复**：
- Scheduler.execute_pipeline → scheduler.run_task()
- API获取模型使用/models接口获取用户可用模型

**影响文件**：
- `gui/theme.py` — 全部重写（HashiCorp配色系统）
- `gui/components/widgets.py` — 全部重写（4种按钮+LanguageMultiSelect+TagLabel）
- `gui/pages/settings_page.py` — 全部重写
- `gui/pages/rules_page.py` — 全部重写（语言多选+优先级修复）
- `gui/pages/dashboard_page.py` — 全部重写
- `gui/pages/history_page.py` — 全部重写
- `gui/app.py` — 侧边栏+固定顶部栏+状态栏+scheduler.run_task()
- `service/dashboard_service.py` — 新增get_dashboard_summary()和run_pipeline()

## 4. 已完成功能详情

### 4.1 定时任务调度集成

- `main.py` 中创建 `TaskService` 实例并调用 `start_scheduler()`
- 使用APScheduler BackgroundScheduler，支持cron和interval两种模式
- `interval_hours≥24`时用cron触发（指定run_time），`<24`时用interval触发
- 应用退出时调用 `stop_scheduler()` 停止调度

### 4.2 开机自启动集成

- 侧边栏CheckBox与`AutoStart.set_autostart()`联动
- 操作Windows注册表`HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`
- 配置变更同步保存到app_config.json

### 4.3 系统托盘（完整实现）

**使用库**：`pystray` + `Pillow`

**功能**：
- 托盘图标（蓝色圆形，PIL动态生成）
- 右键菜单：
  - 显示窗口（默认双击动作）
  - 立即执行
  - 退出
- 通知气泡：
  - 任务完成时弹出"已为您推荐 N 个项目"
  - 任务失败时弹出错误信息
  - 最小化时弹出"应用已最小化到托盘"
- 关闭窗口行为：
  - `minimize_to_tray=True` → 隐藏窗口+托盘通知
  - `minimize_to_tray=False` → 直接退出

**实现方式**：
- 托盘图标在独立守护线程中运行（`threading.Thread(daemon=True)`）
- 通过 `self.after(0, callback)` 从托盘线程安全调用GUI操作
- 退出时调用 `tray_icon.stop()` 停止托盘

### 4.4 首次运行引导集成

- 应用启动时检测`github.token`和`llm.api_key`
- 任一为空时自动弹出引导弹窗
- 引导弹窗支持输入Token/API Key + 创建示例规则
- 示例规则：AI大模型技能（keywords: AI, LLM, Agent, RAG; topics: machine-learning, nlp; language: Python; priority: 8）

### 4.5 自定义MessageBox组件

替代不存在的 `CTkMessagebox`，支持4种图标样式：

| icon参数 | 显示 | 用途 |
| --- | --- | --- |
| "check" | ✅ | 成功提示 |
| "cancel" | ❌ | 失败提示 |
| "info" | ℹ️ | 信息提示 |
| "warning" | ⚠️ | 警告提示 |

### 4.6 PyInstaller打包配置

**文件**：
- `Code/build.spec` — PyInstaller配置文件
- `Code/build.py` — 一键打包脚本

**打包配置**：
- 单文件模式（`EXE` onefile）
- 无控制台窗口（`console=False`）
- 包含数据文件：`config/default_config.json`
- 隐式导入：customtkinter、PIL、github、httpx、bs4、openai、apscheduler、pydantic、loguru、orjson、pystray、win32com等
- UPX压缩启用

**打包命令**：
```bash
cd Code
python build.py
```

### 4.7 边界情况处理

| 模块 | 边界情况 | 处理策略 |
| --- | --- | --- |
| GitHubFetcher | Trending抓取失败 | 降级为Search API查询 |
| RuleMatcher | 无启用规则 | 保留所有仓库，匹配度默认50 |
| RepoEvaluator | 候选仓库为0 | 跳过后续步骤，生成空报告 |
| RepoEvaluator | LLM评估全部失败 | 学习价值得分设为50 |
| RepoEvaluator | 增长数据不可用 | 增长速度得分设为50 |
| LLMSummarizer | JSON解析失败 | 重试一次+默认评分 |
| LLMSummarizer | LLM返回空内容 | 学习价值得分50+降级模板 |
| Scheduler | 任务取消 | 回滚中间数据 |

### 4.8 性能优化

| 优化项 | 实现方式 |
| --- | --- |
| GUI响应性 | 耗时操作在后台线程执行（threading.Thread） |
| API频率控制 | GitHub API保守限制每分钟30次 |
| 超时处理 | httpx连接超时10秒+读取超时30秒，LLM读取超时60秒 |
| 数据库性能 | WAL模式+外键约束+13个索引 |
| 批量操作 | executemany批量插入匹配记录和仓库数据 |

## 5. 待验证项

### 端到端联调（需配置Token后验证）

**验证流程**：
1. 配置GitHub Token → 测试连接成功
2. 配置LLM API Key → 测试连接成功
3. 创建推送规则
4. 点击"立即执行" → 验证9步流水线
5. 仪表盘显示统计数据
6. 历史记录页面显示日志
7. 打开日志文件验证内容

**阻塞原因**：需要有效的GitHub Token和火山方舟API Key。

## 6. 项目整体代码统计

| 层级 | 文件数 | 代码行数 |
| --- | --- | --- |
| 入口 | 1 | 56行 |
| models/ | 5 | 244行 |
| database/ | 3 | 509行 |
| config/ | 1 | 216行 |
| utils/ | 4 | 283行 |
| migrations/ | 1 | 93行 |
| core/ | 5 | 1382行 |
| service/ | 5 | 445行 |
| gui/ | 8 | ~1995行 |
| 打包 | 2 | build.spec + build.py |
| **合计** | **33** | **~5224行** |

## 7. 依赖清单（最终版）

```
customtkinter>=5.2.0
Pillow>=10.0.0
PyGitHub>=2.1.0
httpx>=0.25.0
beautifulsoup4>=4.12.0
openai>=1.0.0
APScheduler>=3.10.0
pydantic>=2.0.0
loguru>=0.7.0
orjson>=3.9.0
pywin32>=306; sys_platform == 'win32'
pystray>=0.19; sys_platform == 'win32'
PyInstaller>=6.0.0
```
